/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package doctomeet.app;

import doctomeet.list.LinkedList;

import static doctomeet.utilities.StringUtils.join;
import static doctomeet.utilities.StringUtils.split;
import static doctomeet.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
